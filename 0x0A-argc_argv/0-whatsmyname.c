#include <stdio.h>
/**
 * main - entry point of program
 * @argc: first operand
 * @argv: second operand
 * Return: 0 (success)
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
printf("%s\n", argv[0]);
printf("\n");
return (0);
}
