#include <stdio.h>
/**
 * main - entry point
 * @argc: first operand
 * @argv: second operand
 * Return: 0 (success)
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d", argc);
printf("\n");
return (0);
}
