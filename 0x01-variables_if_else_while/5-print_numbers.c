#include <stdio.h>
/**
 * main - Entry point
 * This is a program that prints nubers from 0 to 9
 * Return: 0 (success)
 */
int main(void)
{int num;

	num = 0;
while (num < 10)
{printf("%d", num);
	num++;
}
return (0);
}
